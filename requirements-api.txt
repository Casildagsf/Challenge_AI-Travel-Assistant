# Python dependencies for the backend, and enough for the notebook too.
# Render's build command names this file explicitly:
#   pip install -r requirements-api.txt
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
openai>=2.0
requests>=2.31
Babel>=2.14
python-dotenv>=1.0
